# Bounded fixed-basis check, frozen before execution

Exactly four solves: two sequential calls to jax.jit(native fit_amplitudes) in each of the current research environment and the isolated reproduction venv. Both import numerical code from the same pinned 4c5dc6b checkout. Use archived seed-0 final_drift_omega, its 72,000 fitting positions, packaged corrected dataset, native float32 conversion and r/h construction, lambda=.001. No adaptation, warm-up fit, tuning, test inference, random draws or model selection. GPU 0 if idle, existing cooperative lock; identical operational environment in both processes. Current research environment is not an authenticated reconstruction of its September-9 state.

Compute features/Gram/RHS once per process using the native feature function and identical HIGHEST matmul formula, separately from the unmodified native solve. These are diagnostic recomputations, not recovered original training buffers; returning intermediates can have a different compilation context. Compute predictions and float64 diagnostic residual norms using the fixed float32 systems without changing solve precision. Compare repeat/cross-environment arrays exactly and at the already declared rtol=1e-5, atol=1e-6; preserve all deviations. Compare saved amplitudes and OOF targets directly; never infer the exact first arithmetic/Metropolis divergence from unsaved intermediates. Save outputs outside Git, compact hashes/statistics in Git. Stop after these four solves regardless of agreement.
