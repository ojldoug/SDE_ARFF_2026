# Bounded fresh-process minimal reproducer

At most four fresh processes, serially on GPU index 0 verified as UUID GPU-d2cfc73c-186a-6651-e9f1-c6d93a00f0b2 under the existing cooperative lock. Stop as soon as a native amplitude/frequency/prediction difference between processes is found, or a check fails. No full training or compiler/solver/flag sweep.

Extract the already authenticated float32 fold-0 fitting x/y (57600 rows), seed-0 key, zero frequencies and expected first-step frequencies from the preceding bounded capture. Verify every fixture array against that capture's recorded hash before dispatch. Execute native zero-basis initialization and exactly one unchanged compiled native adaptation step, same source and settings; no standalone comparison solve, other fold, selection or warm-up pipeline. This is three source-level amplitude calls per process (initialization, proposal, mixed refit). Capture native coefficients/frequencies, reconstructed feature/Gram/RHS and predictions, lowered/optimized program text and runtime fingerprint if exposed. Reconstructed matrices are not native kernel buffer captures.

Use the same isolated venv and runtime settings as the two full runs. Record actual GPU/driver, loaded library hashes, numerical environment, JAX config/cache defaults, CPU affinity and cache-directory inventories. Historical missing records stay missing. rtol=1e-5 and atol=1e-6 remain unchanged; compare exact bytes too. Matrix diagnostics do not alter solver precision. Preserve both complete runs, all preceding checks, manuscripts and numerical sources.
