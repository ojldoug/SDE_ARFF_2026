# Modern Experiment 8 Joint versus Split MLP ablation

Historical split trainer/checkpoints remain unrecovered. Evidence: results/final_reproduction/HISTORICAL_SPLIT_MLP_AUDIT.md and metric_protocol_reconciliation.md; historical source/notebook searches find manuscript aggregates, not a unique executable procedure. This campaign is a modern controlled ablation, not a reproduction of Owen's historical split MLP.

Both models: 2->27->27->2 drift (893 scalars), 2->27->27->3 lower-triangular covariance factor (921 scalars), tanh hidden activations; total1814. Identical seed-specific initial final-model arrays from the accepted model constructor. Adam lr .001, batch256, 300 epochs per regression, no tuned setting. Canonical data/ex8.npz and80/10/10 indices; split seed2026, run seeds0..29. Dataset/source hashes and effective configuration are frozen in launch_manifest.json. Accepted Joint artifacts remain unchanged.

Joint uses joint Gaussian NLL. Split uses MSE on r/h, five-fold out-of-fold drift residuals, a final full-training drift fit, and Gaussian NLL covariance-factor fitting on frozen residuals. Validation checkpoint minima use drift MSE and covariance NLL respectively, earliest minimum, without test selection. Covariance NLL equals the Joint full NLL conditional on frozen drift. Five nuisance fits add compute;300 epochs per regression is not equal total compute to Joint300 epochs. The extra fits advance the minibatch PRNG stream; identical seed/initialization family does not imply identical minibatch schedules. This compares complete joint versus cross-fitted two-stage procedures.

Static syntax and git diff --check passed. CPU synthetic40/10/10 two-epoch smoke exercised initialization, warm-up, all7 regressions, final metrics and native schema4 serialization/round-trip;1814 parameters and identical Joint initialization verified. Supervisor mocked success/failure/reuse tests passed, preserving suspicious output. All120 prior accepted artifacts/logs validated before launch. All4 GPUs showed0% utilization,1MiB and no compute processes before launch.

Four exclusive GPUs; seed modulo4, no simultaneous benchmark jobs per GPU; per-dispatch occupancy checks. Timings are non-isolated parallel accuracy measurements. Existing numerical timing semantics preserved (reported end-to-end=warm-up+algorithm, excluding final evaluation/serialization/loading). No numerical module or global config modified.
